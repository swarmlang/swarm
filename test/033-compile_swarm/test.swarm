fn x = (arg1: number, arg2: number): number => {
    while (arg1 > 0) {
        arg1 -= arg2;
        if (arg2 < 0) {
            break;
            if (arg1 == 30) {
                continue;
            }
        }
    }
    return 0;
};
name: C/C++ CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:

    runs-on: glmdev/swarm-ci:latest

    steps:
    - uses: actions/checkout@v2
    - name: make all
      run: make all
